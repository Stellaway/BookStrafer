<Page
    x:Class="Cookbook.Views.DetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cookbook.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:controls="using:Template10.Controls"
    xmlns:vm="using:Cookbook.ViewModels"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Page.DataContext>
        <vm:DetailsPageViewModel x:ConnectionId='2' x:Name="ViewModel"  />
    </Page.DataContext>

    <Page.Resources>
        <CollectionViewSource x:Key="AuthorsViewSource" Source="{Binding Authors}" ItemsPath="Authors" />
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <controls:PageHeader Grid.Row="0" Content="{Binding Book.title}" />
        

        <StackPanel Grid.Row="1" Margin="18" Orientation="Vertical" >
            <Image x:ConnectionId='3'  x:Name="clickableCover"                                 Source="{Binding MediumCover}" Stretch="Uniform" Height="200" HorizontalAlignment="Left" />
            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}">Author(s)</TextBlock>

            <GridView x:ConnectionId='4' Grid.Row="1"
  Padding="18"
   ItemsSource="{Binding Source={StaticResource AuthorsViewSource}}"
  IsItemClickEnabled="True"
                                                  >
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Border VerticalAlignment="Bottom" Background="#AA000000">
                                <TextBlock Text="{Binding name}" Margin="12" Foreground="White"/>

                            </Border>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>

            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}"
Text="Description" Margin="0,0,0,12" />
            <!-- create a textblock for book description -->
            <TextBlock FontSize="12" Text="{Binding Book.description}"  TextWrapping="WrapWholeWords"/>
        </StackPanel>
    </Grid>
</Page>

